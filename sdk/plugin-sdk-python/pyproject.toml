[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "aitest-plugin-sdk"
version = "0.1.0"
description = "aitest PLG Python SDK"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
